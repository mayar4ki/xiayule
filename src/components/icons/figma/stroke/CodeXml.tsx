import type { FigmaIconProps } from "../types";
import { baseProps } from "../util";

export function FigmaIconCodeXml(props: FigmaIconProps) {
  return (
    <svg viewBox="1498.5 300.5 15.000 15.000" {...baseProps(props)}>
      <path
        d="M1512 302L1500 314M1500 302L1512 314"
        stroke="currentColor"
        strokeWidth={1.5}
        strokeLinecap="round"
        strokeLinejoin="round"
      />
    </svg>
  );
}
