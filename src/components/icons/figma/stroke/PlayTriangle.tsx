import type { FigmaIconProps } from "../types";
import { baseProps } from "../util";

export function FigmaIconPlayTriangle(props: FigmaIconProps) {
  return (
    <svg viewBox="1550.5 303.5 8.000 9.000" {...baseProps(props)}>
      <path
        d="M1552 311L1557 308L1552 305V311Z"
        stroke="currentColor"
        strokeWidth={1.5}
        strokeLinecap="round"
        strokeLinejoin="round"
      />
    </svg>
  );
}
